// Single source of truth for the canonical production URL.
// Used by SEOHead (canonical + Open Graph), the sitemap, and anywhere else
// that needs an absolute URL. If you move to a custom domain (e.g.
// https://migrent.com.au), change it here and update public/robots.txt.
export const SITE_URL = "https://migrent.vercel.app";
