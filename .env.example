# This repository has two deployable apps with their own environment files:
#   backend/.env.example    FastAPI on Render (server secrets live here)
#   frontend/.env.local.example  Next.js on Vercel (NEXT_PUBLIC_* are public)
# Nothing in this root file is read by either app.
